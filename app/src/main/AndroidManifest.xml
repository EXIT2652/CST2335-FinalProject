<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hliu.cst2335_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityTrackingActivity" />
        <activity android:name=".PkgFood.FoodInformationActivity">
         android:label="@string/food_app_name_f"</activity>
        <activity android:name=".HouseThermostatActivity" />
        <activity android:name=".AutomobileActivity" />
        <activity android:name=".PkgFood.FoodAddActivity"></activity>
        <activity android:name=".PkgFood.FoodFragmentListView"></activity>
    </application>

</manifest> <!-- android:theme="@style/AppTheme"> -->
